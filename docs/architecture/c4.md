# Arquitetura — C4 (contexto e contêineres)

**Versão:** 1.0.0 | **Estado:** implemented

Os dois primeiros níveis de C4 são suficientes para esta escala.

## Contexto

```mermaid
flowchart LR
  user["Pessoa desenvolvedora"] -->|"registra e acompanha experimentos"| system["LabSpec"]
  system -->|"lê especificações e código"| repo["Repositório Git"]
```

## Contêineres

```mermaid
flowchart LR
  browser["Browser"] -->|"HTTPS/JSON em produção; HTTP local"| web["Web React/Vite"]
  web -->|"API /api/v1; contrato OpenAPI"| api["API Fastify"]
  api -->|"interface ExperimentRepository"| memory["Armazenamento em memória"]
  contracts["Pacote contracts / Zod"] --> web
  contracts --> api
```

## Fronteiras

- Browser → Web/API: entrada não confiável.
- Web → API: CORS não é autorização; todo corpo é validado no servidor.
- API → repositório: interface substituível; domínio não conhece tecnologia de persistência.
- Ambiente → processo: configuração validada na inicialização; segredos nunca usam prefixo `VITE_`.
